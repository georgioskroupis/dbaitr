# Repo context (optional). If unset, script infers from git remote origin.
# GH_REPO=georgioskroupis/dbaitr

# Firebase
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SERVICE_ACCOUNT_FILE=/absolute/path/to/firebase-service-account.json

# GCP
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCP_WORKLOAD_IDENTITY_PROVIDER=projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/github-pool/providers/github-provider
GCP_SERVICE_ACCOUNT_EMAIL=github-cloud-run-deployer@your-gcp-project-id.iam.gserviceaccount.com

# Web runtime env (used at build time in GitHub Actions)
# Firebase Web Config
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Personhood verification
IDV_SELF_VERIFY_URL=https://your-idv-verifier.example.com/verify
# Optional session bootstrap endpoint (returns verification URL)
# IDV_SELF_START_URL=https://your-idv-verifier.example.com/start
# Optional API key sent as X-Idv-Api-Key
# IDV_SELF_VERIFY_API_KEY=replace-me
IDV_DEDUP_HMAC_SECRET=replace-with-long-random-secret
IDV_CHALLENGE_TTL_MS=600000
IDV_SELF_VERIFY_TIMEOUT_MS=15000
